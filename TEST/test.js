const arr = [];
const num = 0;
function getSum(numbers) {
  numbers.reduce((a, b) => a + b);
  arr.push(s);
  numbers.map((e) => {});
  return arr;
}

console.log(getSum([4, 2, 1, 5]));
//! [12,8,6,5,0 ]
